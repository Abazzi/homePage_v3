export type NavBarProps = {
  theme: string;
  setTheme: React.Dispatch<React.SetStateAction<string>>;
}

export type PortraitProps = {
  theme: string;
  setTheme: React.Dispatch<React.SetStateAction<string>>;
}
